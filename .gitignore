# Google OAuth / Firebase downloads — keep local only (copy into app manually)
assets-to-copy-from/client_secret*.json
assets-to-copy-from/GoogleService-Info*.plist
assets-to-copy-from/google-services*.json
**/GoogleService-Info*.plist
!**/GoogleService-Info.plist.example
**/google-services.json
!**/google-services.json.example

# Secrets — never commit
Backend/.env
Backend/cloud-run.env
Backend/cloud-run.env.yaml

# Backend dependencies — install with `npm ci` in Backend/ (Dockerfile does this for deploy)
Backend/node_modules/
