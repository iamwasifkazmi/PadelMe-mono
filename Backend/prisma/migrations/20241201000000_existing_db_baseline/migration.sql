-- Baseline migration: the database already had the full schema before Prisma Migrate was introduced.
-- Mark this as applied without running (see Backend prisma baseline instructions):
--   npx prisma migrate resolve --applied "20241201000000_existing_db_baseline"
-- Then run: npx prisma migrate deploy

SELECT 1;
